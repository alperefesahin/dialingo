/*


class SpeakViewBodyWidget extends StatelessWidget {
  const SpeakViewBodyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    final size = MediaQuery.sizeOf(context);

    return
  }
}
*/
